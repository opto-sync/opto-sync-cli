[package]
org = "opto-sync"
name = "opto-sync-cli"
version = "0.1.0"
kind = "cli"
description = "Read-only validation, replay, and redacted diagnostics for Opto Sync"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/opto-sync/opto-sync-cli"

[dependencies]
"oresoftware/ores-clis-core" = "^0.1.0"

[targets.rust]
dir = "."
adapter = "rust"

[publish]
enabled = false
